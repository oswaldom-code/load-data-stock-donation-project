# load-data-stock-donation-project

Commads:

```
commands:
./run.sh cli --function load --target file --path file_name.json

Or

./bin/load-data --function load --target file --path file_name.json
```
